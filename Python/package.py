# Python can organize modules into packages (similar to Java).
# You then use the package.subpackage.module notation to access them.
# For a directory to be treated as a package, it must contain the file __init__.py

