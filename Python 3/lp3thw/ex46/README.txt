We use virtualenv and virtualenvwrapper to get virtual environments.
These allow us to have different package deps for different projects.
These allow us to not contaminate system production Python packages with dev packages.

We use the nose package to run unit tests.

We define a project skeleton directory structure.


